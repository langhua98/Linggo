# 词根数据说明（词根.json）

100 个拉丁/希腊语词根的**事实性字段**，用于「词霸天下38000之一」词书的
词根总览卡、词族回忆卡和对照表表头。

| 字段 | 含义 | 示例 |
|------|------|------|
| `no` | 序号，与词书章节顺序一致 | `1` |
| `root` | 词根及其同源异形变体 | `-vinc- = -vict-` |
| `mean` | 核心词义 | `胜，征服` |
| `extend` | 引申义项 | `战胜/胜利/征服/克服` |
| `src` | 源语言、源词、词义 | `{lang:"拉丁语", word:"vincere", gloss:"战胜"}` |

## 内容边界

只收录**词源事实**——某个词根来自哪个拉丁/希腊语词、那个词是什么意思。
这类信息在任何词源词典里都一致，不属于任何人的独创表达。

不收录任何第三方教材的行文、考据段落、例句或讲解。

## 准确性

`src` 字段由程序初抽后逐条复核。判据是「源词应以词根变体开头」，
据此筛出 11 条可疑 + 7 条缺失，全部人工核订，例如：

- `-od-`（路）初抽成 `methodus`，实为希腊语 `hodos`——methodus 是 meta+hodos 的复合词
- `-lat-` 初抽成片段 `ger`，实为拉丁语 `latus`（ferre「搬运」的过去分词词干）
- `-sum-` 初抽成 `super`，实为 `summus`（最高）

21 条只有源词、没有词义，保持留空——宁可少显示一行，也不编造词源。
