# Linggo 更新日志

本文件记录 Linggo 的迭代改动，按主题归类。Service Worker 缓存版本随每次前端改动递增（当前 `linggo-v126`）。

---

## 本轮迭代（SW v104 → v126）

### 📚 书库扩展与在线搜索
- **书目从 130 扩到 200+**：Standard Ebooks 精选从 12 本扩展到 92 本（奥斯汀、狄更斯、哈代、乔治·艾略特、托尔斯泰、陀思妥耶夫斯基、柯南·道尔全系列、威尔斯、凡尔纳、杰克·伦敦、勃朗特三姐妹等），与 Gutenberg 本地目录一起在浏览页展示。
- **Gutendex 在线实时搜索**：输入关键词时，先即时显示本地 200+ 本，再后台调用 Gutendex API（`gutendex.com`，7 万本 Gutenberg 全库，支持 CORS）追加在线结果，去重后合并。
- **分类在线浏览**：点分类标签（经典/推理/冒险/科幻/恐怖/短篇）即使不输关键词，也从 Gutenberg 全库按 `topic` 拉取该类别（按下载量排序，排前面的都是该类经典）。
- **搜索框合并**：去掉浏览面板里重复的搜索框，统一由顶部搜索栏驱动。
- **浏览页铺满视口**：结果网格填满屏幕高度，不再被压在上半屏、下方留白。

### 🎨 图标动画（Lottie）
- 接入 **useAnimations**（MIT 协议，全部本地托管、离线可用）+ lottie-web 引擎：
  - 播放器：播放↔暂停 morph、上一句/下一句
  - 顶部栏：搜索↔✕、设置齿轮、章节菜单、生词书签
  - 单词弹窗：加入生词时爱心弹跳
- 颜色用 `currentColor` 强制覆盖 Lottie 内置色，夜间模式/强调色自动适配；Lottie 未加载成功时回退到原 SVG。
- 新增 `icon-preview.html`：全部 79 个图标动画的预览页（分类、明暗切换、悬停播放）。

### 🔊 Kokoro 神经语音
- **冷启动倒计时**：服务器冷启动时（>5s 无响应），播放键上方显示倒计时药丸并后台继续等待，拿到语音立即播放；区分"服务器启动中"与"正在生成语音"两种文案。
- **逐词精确高亮（真实时间戳）**：服务器新增 `/tts-timed` 端点，返回音频 + 每个词的 `[起,止]` 时间（取自 Kokoro `result.tokens[].start_ts/.end_ts`）；客户端据此精确高亮，拿不到时退回线性估算。
- **`/tts-timed` 服务器缓存**：独立 LRU 缓存，重复请求秒回（实测约 16 倍提速），不再重新合成。
- 高亮循环从 `timeupdate`（~4次/秒，卡顿滞后）改为 **`requestAnimationFrame` 60fps** 轮询 + 50ms 提前量补偿。

### 🎭 UI 精致化与特效（均纯增量、可回退、尊重 `prefers-reduced-motion`）
- **原生 View Transitions**：首页↔阅读器切换的丝滑淡入淡出（轻微缩放）。
- **封面共享元素飞入**：点书卡时封面 morph 进详情弹窗（解决 View Transitions 命名冲突）。
- **首页 Hero 错峰渐入** + **粒子背景**（手写 canvas，跟随主题、隐藏时暂停）。
- **书卡封面光泽扫过**（hover）+ **详情页封面光泽**（打开时扫过一次）。
- **句子高亮**升级为暖琥珀色高亮笔质感 + 柔和浮起阴影 + "墨迹沉淀"脉冲。
- **卡拉OK 滑动下划线**：当前朗读词下方光条平滑跟随。
- **进度条液体波纹**（播放时流动光泽）、**章节翻页惯性**、**播放器句子预览淡切**、**单词弹窗魔法出现**、**成就彩带**（读完整本书 / 闪卡掌握率 ≥80%，手写 canvas）。

### 🐛 Bug 修复
- **存储型 XSS**：`buildCard`/`buildSearchCard` 未转义就把 Gutendex 书名/作者拼进 `innerHTML`（在线搜索引入的不可信元数据），改用 `_esc()` 转义。
- **暂停无法停止兜底系统语音**：Kokoro 模式下走 speechSynthesis 兜底时（iOS 冷启动常见），暂停只停了 `<audio>` 元素、系统语音继续念且恢复时重叠——改为按当前引擎判断，兜底路径硬停。
- **卡拉OK下划线不协调**：跨行/跨句时斜穿屏幕 → 同行内滑动、换行换句瞬间吸附；页面自动下滑时跟随滚动；换句时先隐藏，避免滞留在上一句。
- **禁止捏合缩放**：iOS Safari 故意无视 `user-scalable=no`，改用 `touch-action:pan-x pan-y` + `gesturestart`/多指 `touchmove` 拦截。
- **首页布局被撑破**：粒子 canvas 作为替换元素在绝对定位下用了固有像素尺寸（`clientWidth×dpr≈780px`）当显示尺寸导致横向/纵向溢出，加 `width/height:100%` 约束显示尺寸（绘图缓冲仍高清）。

### 🚀 基础设施
- Kokoro 服务器（HF Space `langhua1998/kokoro`）新增 `/tts-timed` 端点并部署上线；`/tts`、`/tts-stream` 保持不变，向后兼容。
